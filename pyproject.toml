[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "lmdb"
version = "0.0.0"
description = "Wikipedia movie DB helpers"
requires-python = ">=3.9"

[tool.setuptools]
packages = ["utils"]

